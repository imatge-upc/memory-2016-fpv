function optimize()


end

function Accuracy = evaluate(MAT, thW, thBl, thBr)

for i = 1: size(MAT,2)
    if(blur > th || dark < self.thresholdDark || dark > self.thresholdBright)
    end
end

end